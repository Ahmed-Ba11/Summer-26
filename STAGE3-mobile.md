# البند 3 (معدّل) — الهيكل المتجاوب · الجوال أولاً

> يحلّ محل البند 3 في `STAGE3.md`. **لا تنفّذ ذاك، نفّذ هذا.**
> `AppRail.svelte` القديم **ملغى** — استُبدل بـ `AppNav.svelte`.

---

## القرار المعتمد

**الجوال هو التصميم الأساسي، والديسكتوب توسّع له — لا العكس.**

هذا يقلب طريقة البناء: الجدول لا يُبنى ثم يُحوَّل للجوال. يُبنى **بطاقات
أولاً**، ثم يصير جدولاً عند `md:` فأعلى.

---

## ما وجدته في الفحص

| المشكلة | الصفحات |
|---|---|
| جداول بتمرير أفقي على الجوال | `Expenses` · `Income` · `Reports` |
| صفر نقاط كسر `md:` — لا معالجة تابلت | 6 صفحات |
| مبنية ديسكتوب-أولاً بالكامل | `Welcome` — 20 `lg:` وصفر `sm:` |
| مودالات لا تتحوّل للوح سفلي | كل الصفحات القديمة |
| أهداف لمس أصغر من 44px | أزرار الحذف والتعديل |

---

## الملفات الجاهزة

```
resources/js/components/AppNav.svelte        ← الهيكل المتجاوب (يستبدل AppRail)
resources/js/components/MobileHeader.svelte  ← رأس الشاشة على الجوال
```

**لا تعيد كتابتهما.** مطابقان للنموذج البصري المعتمد.

---

## التخطيط

**الجوال (< 768px)**

```
┌─────────────────────────────┐
│ رأس لاصق: العنوان · [مساعد] │
├─────────────────────────────┤
│                             │
│         المحتوى             │
│      (بطاقات، لا جداول)     │
│                             │
├─────────────────────────────┤
│ الرئيسية الميزانية [+] الالتزامات المزيد │
└─────────────────────────────┘
```

- زر **[+]** في منتصف الشريط السفلي — أقرب نقطة للإبهام، وأهم إجراء يومي
- **«المزيد»** يفتح لوحاً منزلقاً: المعاملات · الادخار · التقارير · المساعد · الإعدادات
- **المساعد** في رأس الشاشة، لا في الشريط — يبقى متاحاً بلا ما يستهلك خانة

**الديسكتوب (≥ 768px)**

الشريط السفلي والرأس يختفيان، ويظهر الشريط الجانبي 78px القابل للتوسّع
إلى 252px. لا شيء آخر يتغيّر — نفس المكوّنات ونفس الأرقام.

---

## الرسالة — الصقها في opencode

> نفّذ **البند 3 المعدّل** من `STAGE3-mobile.md`. الهدف: هيكل متجاوب،
> الجوال أولاً.
>
> `AppNav.svelte` و `MobileHeader.svelte` موجودان وجاهزان — **لا تعيد
> كتابتهما ولا تعدّل تصميمهما**. `AppRail.svelte` ملغى — احذفه.
>
> ---
>
> **1 · استبدل الهيكل**
>
> في `resources/js/layouts/app/AppSidebarLayout.svelte`:
> - استبدل `AppSidebar` بـ `AppNav`
> - أضف حشوة سفلية للمحتوى على الجوال حتى لا يغطيه الشريط السفلي:
>   `pb-[calc(72px+env(safe-area-inset-bottom))] md:pb-0`
> - مرّر `onQuickAdd` لـ `AppNav` بحيث يفتح `QuickAddSheet` نفسه الذي
>   يفتحه `QuickAddFab`
>
> احذف نهائياً: `AppSidebar.svelte` · `NavMain.svelte` · `AppRail.svelte`
>
> ---
>
> **2 · زر الإضافة**
>
> على الجوال يعيش داخل الشريط السفلي (موجود في `AppNav`). على الديسكتوب
> يبقى `QuickAddFab` عائماً. اجعل `QuickAddFab` مخفياً على الجوال:
> `hidden md:flex`. **زر إضافة واحد ظاهر في كل مقاس** — لا اثنان.
>
> ---
>
> **3 · المشاركة العامة**
>
> أضف `navStats` إلى `HandleInertiaRequests::share()`:
> ```php
> 'navStats' => $request->user() ? [
>     'remaining' => ..., 'dailySafe' => ..., 'daysLeft' => ...,
>     'budgetUsedPct' => ..., 'transactionsCount' => ...,
>     'dueCommitments' => ..., 'savingsPct' => ...,
>     'incomeSplit' => [ ['key'=>'bills','pct'=>18,'color'=>'var(--chart-7)'], ... ],
> ] : null,
> ```
> واقرأ كوكي `rail_expanded` ومرّرها كـ prop أوّلي `expanded` لتفادي وميض
> التخطيط عند التحميل.
>
> ---
>
> **4 · رأس الشاشة**
>
> استخدم `MobileHeader` في أعلى كل صفحة، ومرّر لها العنوان وسطر السياق.
> وأخفِ رأس الصفحة الحالي (العنوان الكبير + الوصف) على الجوال بـ
> `hidden md:block` حتى لا يتكرر العنوان مرتين.
>
> ---
>
> **5 · المسارات المدموجة**
>
> أنشئ `/transactions` (تبويبا مصاريف/دخل) و `/commitments` (تبويبا
> فواتير/أقساط). أبقِ `/expenses` و `/income` و `/bills` و `/installments`
> كإعادة توجيه دائمة (301) للحفاظ على أي رابط قديم.
>
> ---
>
> **6 · الأيقونات**
>
> استبدل `Sparkles` بـ `AiAssistantIcon` و `PiggyBank` بـ `Vault` في كل
> المشروع بلا استثناء.
>
> ---
>
> **7 · قواعد الجوال — طبّقها على كل صفحة تلمسها**
>
> - **كل جدول** يُعرض كبطاقات على الجوال وجدولاً عند `md:` فأعلى، بالنمط:
>   `<table class="hidden w-full md:table">` و `<ul class="md:hidden">`.
>   **ممنوع `overflow-x-auto` على الجوال** — صفر تمرير أفقي.
> - **كل هدف لمس** لا يقل عن `min-h-11 min-w-11` (44px).
> - **كل مودال** يفتح كلوح سفلي على الجوال (`items-end` + `rounded-t-3xl`)
>   وفي المنتصف على الديسكتوب (`md:items-center` + `md:rounded-3xl`).
> - **كل شبكة** تبدأ بعمود أو عمودين، وتتوسّع عند `md:` ثم `lg:`.
> - احترم `env(safe-area-inset-bottom)` في أي عنصر ثابت أسفل الشاشة.
>
> ---
>
> **8 · تحقّق**
>
> `npm run build` ثم `php artisan test --compact`. ثم افتح أدوات المطوّر
> على عرض **375px** وتأكّد أن كل صفحة:
> - لا تمرير أفقي إطلاقاً
> - الشريط السفلي لا يغطي أي محتوى
> - كل زر قابل للضغط بالإبهام
>
> **قواعد ملزمة:** اقرأ `AGENTS.md` و `DESIGN.md` أولاً. لا تعدّل أي ملف
> في `resources/js/components/` عدا `QuickAddFab` و `AppSidebarLayout`.
> المبالغ بالهللات.
>
> وقف عندي بعد البند 1 و 2 واعرض الملفات المتغيّرة قبل ما تكمل.

---

## الاختبار بعد التنفيذ

```
composer run dev
```

**على الديسكتوب:** الشريط الجانبي الرفيع + الحلقة، واضغط الحلقة ليتوسّع.

**على الجوال:** افتح أدوات المطوّر (F12) → أيقونة الجوال → اختر
**iPhone SE (375×667)**، وتحقّق:

| # | الاختبار | المتوقع |
|---|---|---|
| 1 | الشريط السفلي ظاهر | 4 وجهات + زر [+] في المنتصف |
| 2 | اضغط «المزيد» | لوح ينزلق من الأسفل |
| 3 | اضغط [+] | لوح الإضافة السريعة يفتح من الأسفل |
| 4 | مرّر أي صفحة يميناً ويساراً | **لا يتحرك شيء** — صفر تمرير أفقي |
| 5 | المساعد | أيقونة العملات + الشرارة في رأس الشاشة |

```
git add -A
git commit -m "feat: stage 3 - mobile-first responsive shell"
git push
```

---

## بعدها — المرحلة 4

كل الصفحات الداخلية mobile-first، وتوحيد المودالات، وبطاقات الالتزامات،
وسؤال الفاتورة المتغيّرة. وستدمج معها ملاحظاتك البصرية.

**والمرحلة 5:** تحويل التطبيق إلى PWA قابل للتثبيت.
