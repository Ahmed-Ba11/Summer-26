# المرحلة 2 — المصروف الأكبر من الدخل

**النموذج:** القوي · **السبب:** إعادة تصميم تدفّق يراه المستخدم في أحرج لحظة.

**الهدف:** تحويل شاشة واحدة مكدّسة تطلع عن حدود الشاشة إلى **خطوتين**.

## قواعد ثابتة — التزم بها حرفياً في هذه المرحلة

**التصميم (لا تحتاج فتح `DESIGN.md` — كل ما يلزمك هنا):**
- بطاقات: `rounded-2xl border border-border bg-card shadow-xs`
- مسافات: `p-3 gap-3` جوال · `md:p-6 md:gap-5` ديسكتوب
- عناوين `text-[14px] font-semibold` · نص مساند `text-[11.5px] text-muted-foreground`
- **لا نص أصغر من 11px · لا إيموجي · لا تمرير أفقي · كل زر `min-h-11`**
- تنسيقات منطقية فقط (`ms/me/ps/pe/start/end`) — ممنوع `left/right`
- **أرقام لاتينية دائماً** — لا `٠١٢٣` ولا تواريخ عربية-هندية
- أيقونة في بطاقة: حاوية 38–40px والرمز 19–20px · في زر أو رأس: 17–18px
  · شريط التنقّل 24px · زر «+» 58px · `stroke-width: 1.9`
- **إحساس تطبيق جوال**: كل مودال لوح سفلي، أزرار متجاورة لا مكدّسة،
  `active:scale-[.98]` عند الضغط، احترام `env(safe-area-inset-*)`
- كل المبالغ **بالهللات (integer)** — التحويل في `@/lib/format` فقط

**اقتصاد السياق:**
- لا تقرأ المشروع كاملاً. ابحث أولاً (grep) ثم افتح الملفات التي ظهرت فقط.
- لا تفتح ملفاً «لفهم المشروع أكثر». لا تقرأ `node_modules` · `vendor` · `dist` · lock files.
- لا تعِد قراءة ملف قرأته في هذه الجلسة إلا إن تغيّر.
- لا تشرح الكود لي ولا تعرض محتوى الملفات ولا diff كامل — نفّذ مباشرة.
- لا تعمل refactor خارج نطاق هذه المرحلة.
- استعمل المكوّنات الموجودة، ولا تعيد كتابة مكوّن المطلوب إعادة استخدامه فقط.
- ردودك أثناء التنفيذ مختصرة جداً.

**إن فشل نفس الإصلاح مرّتين:** توقّف، واذكر في ثلاثة أسطر: المشكلة · الملفات ·
ما جُرّب. لا تحاول ثالثة.

---

## ابدأ بالبحث عن

```
ExpenseFundingService     FundingSourcePicker     QuickAddSheet
checkExpense              availableToSpend
```

اقرأ فقط: مكوّن الإضافة السريعة · `FundingSourcePicker` · توقيع
`ExpenseFundingService` (لا تقرأ الخدمة كاملة إن لم تحتج تعديلها).

---

## التنفيذ

### الخطوة 1 — الإدخال

- `AmountSheet` + شريط الفئات + حقل الوصف
- **سطر تحذير واحد فقط**، الأشدّ خطورة، **فوق لوحة الأرقام** ليبقى ظاهراً
  بلا تمرير. رابط «التفاصيل» يكشف بقيّة التحذيرات عند الطلب.
- اقتراح التصحيح («تقصد 2,000.4؟») **منفَّذ داخل `AmountSheet` أصلاً** —
  لا تكرّره كبطاقة مستقلة.
- زر التذييل يصير **«التالي»** بدل «حفظ» إذا تجاوز المبلغ المتبقي.

### الخطوة 2 — «من وين جاء المبلغ؟»

- `SheetShell` بـ `showBack` و `steps={2}` و `currentStep={2}`
- بطاقة الفجوة بالأحمر في الأعلى: «مصروف X يتجاوز المتبقي لك» + الرقم كبيراً
- ثلاثة خيارات بارتفاع **70px**: من مدخراتي · دخل ما سجّلته · تجاوزت وأعرف
- الخيار غير المتاح **`disabled` مع ذكر السبب** («ما فيه هدف رصيده يكفي») —
  لا يُخفى
- تحذير الميزانية الثانوي أسفل الخيارات كخبر لا كمنافس على الانتباه

**الخادم:** منطق `ExpenseFundingService` يبقى كما هو — التغيير في العرض فقط.
إن اكتشفت أن الخادم لا يدعم التدفّق، أصلحه بأقل تغيير ممكن واذكر ذلك.

---

## التحقّق

```
npm run build
```

ثم على 375px: أدخل مبلغاً أكبر من المتبقي → شاشة الإدخال **مرتّبة بلا خروج
عن الحدود** وفيها تحذير واحد → «التالي» → شاشة مصدر التمويل وحدها.

---

## عند الانتهاء

اعرض **فقط**:
1. ما نُفِّذ (نقاط قصيرة)
2. أسماء الملفات المتغيّرة
3. نتيجة التحقّق
4. أي شيء يمنع المرحلة التالية

ثم **توقّف. لا تبدأ المرحلة التالية.**

```
git add -A
git commit -m "feat(stage-2): two-step expense funding flow"
```
