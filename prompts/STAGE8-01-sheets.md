# المرحلة 1 — توحيد الألواح

**النموذج:** القوي · **السبب:** refactor بصري يمسّ كل صفحات التطبيق،
وأي انحراف هنا يتكرّر في كل مكان.

**الهدف:** كل مودال ولوح في التطبيق يُبنى على `SheetShell`، وكل إدخال مبلغ
أو تاريخ أو يوم شهر يمرّ على المكوّن الموحّد الخاص به.

## قواعد ثابتة — التزم بها حرفياً في هذه المرحلة

**التصميم (لا تحتاج فتح `DESIGN.md` — كل ما يلزمك هنا):**
- بطاقات: `rounded-2xl border border-border bg-card shadow-xs`
- مسافات: `p-3 gap-3` جوال · `md:p-6 md:gap-5` ديسكتوب
- عناوين `text-[14px] font-semibold` · نص مساند `text-[11.5px] text-muted-foreground`
- **لا نص أصغر من 11px · لا إيموجي · لا تمرير أفقي · كل زر `min-h-11`**
- تنسيقات منطقية فقط (`ms/me/ps/pe/start/end`) — ممنوع `left/right`
- **أرقام لاتينية دائماً** — لا `٠١٢٣` ولا تواريخ عربية-هندية
- أيقونة في بطاقة: حاوية 38–40px والرمز 19–20px · في زر أو رأس: 17–18px
  · شريط التنقّل 24px · زر «+» 58px · `stroke-width: 1.9`
- **إحساس تطبيق جوال**: كل مودال لوح سفلي، أزرار متجاورة لا مكدّسة،
  `active:scale-[.98]` عند الضغط، احترام `env(safe-area-inset-*)`
- كل المبالغ **بالهللات (integer)** — التحويل في `@/lib/format` فقط

**اقتصاد السياق:**
- لا تقرأ المشروع كاملاً. ابحث أولاً (grep) ثم افتح الملفات التي ظهرت فقط.
- لا تفتح ملفاً «لفهم المشروع أكثر». لا تقرأ `node_modules` · `vendor` · `dist` · lock files.
- لا تعِد قراءة ملف قرأته في هذه الجلسة إلا إن تغيّر.
- لا تشرح الكود لي ولا تعرض محتوى الملفات ولا diff كامل — نفّذ مباشرة.
- لا تعمل refactor خارج نطاق هذه المرحلة.
- استعمل المكوّنات الموجودة، ولا تعيد كتابة مكوّن المطلوب إعادة استخدامه فقط.
- ردودك أثناء التنفيذ مختصرة جداً.

**إن فشل نفس الإصلاح مرّتين:** توقّف، واذكر في ثلاثة أسطر: المشكلة · الملفات ·
ما جُرّب. لا تحاول ثالثة.

---

## ابدأ بالبحث عن (لا تفتح شيئاً قبل نتائج البحث)

```
Dialog          type="date"        <select
svelte-ignore   a11y_              fixed inset-0
```

في `resources/js/pages` و `resources/js/components` فقط.

---

## المكوّنات الجاهزة

| المكوّن | يُستعمل في |
|---|---|
| `@/components/ui/SheetShell.svelte` | **كل** مودال/لوح — رأس ثابت + جسم يتمرّر + تذييل ثابت |
| `@/components/ui/AmountSheet.svelte` | **كل** إدخال أو تعديل مبلغ |
| `@/components/ui/DateSheet.svelte` | **كل** إدخال أو تعديل تاريخ |
| `@/components/ui/DayOfMonthPicker.svelte` | اختيار يوم من الشهر |

اقرأ `SheetShell.svelte` و `AmountSheet.svelte` مرة واحدة لتعرف واجهتهما،
ثم استعملهما بلا إعادة قراءة.

---

## التنفيذ — صفحة مرجعية أولاً ثم تكرار

### الدفعة A — المصاريف (المرجع)

حوّل لوح إضافة/تعديل المصروف إلى `SheetShell` + `AmountSheet`.
ثم `npm run build` وتوقّف عند هذه النقطة للحظة: **هذه الصفحة هي المرجع
البصري لكل ما بعدها.** لا تنتقل قبل أن تكون مطابقة للقواعد أعلاه.

### الدفعة B — الدخل والميزانيات
### الدفعة C — الادخار والالتزامات
### الدفعة D — بقيّة المودالات (تأكيد الحذف · تعديل فاتورة من التقويم المالي · أي مودال متبقٍّ)

في B و C و D **كرّر نفس البنية حرفياً** من الدفعة A. لا تبتكر تخطيطاً جديداً.
بعد كل دفعة: `npm run build` فقط. لا تعِد فحص الدفعات السابقة إلا عند ظهور
خلل فيها.

---

## ما يجب أن تحلّه هذه المرحلة (أخطاء مُبلَّغة فعلياً)

| الخطأ | السبب | الحل |
|---|---|---|
| زر X يتداخل مع العنوان — متكرّر في صفحات كثيرة | العنوان بلا `min-w-0` والزر بلا `shrink-0` | `SheetShell` |
| زر التأكيد لا يظهر في «إضافة التزام» | كان آخر عنصر في نموذج طويل | تذييل `SheetShell` خارج منطقة التمرير |
| تعديل مبلغ الفاتورة بدائي | حقل نصّي خام | `AmountSheet` |
| تعديل التاريخ سيّئ وبأرقام عربية | `<input type="date">` | `DateSheet` |
| قائمة اليوم المنسدلة تغطّي الشاشة | `<select>` بـ 31 عنصراً | `DayOfMonthPicker` |
| الأيقونات صغيرة | مقاسات قديمة | المقاسات في القواعد أعلاه |

---

## التحقّق

```
npm run build
```

ثم ابحث للتأكّد أن هذه **اختفت من `resources/js`**:
`type="date"` · `<select` لأيام الشهر · `svelte-ignore a11y_`

وافحص صفحتين على عرض 375px: صفر تمرير أفقي · زر الحفظ ظاهر · X لا يتداخل.

---

## عند الانتهاء

اعرض **فقط**:
1. ما نُفِّذ (نقاط قصيرة)
2. أسماء الملفات المتغيّرة
3. نتيجة التحقّق
4. أي شيء يمنع المرحلة التالية

ثم **توقّف. لا تبدأ المرحلة التالية.**

```
git add -A
git commit -m "feat(stage-1): unify sheets"
```
