# المرحلة 4 — شهر الراتب

**النموذج:** القوي · **السبب:** منطق يمسّ كل وحدة مالية في التطبيق.

**الهدف:** شهر المستخدم يبدأ يوم نزول راتبه لا يوم 1، فتبقى أهم الأرقام
صحيحة طول الشهر بدل أن تُصفَّر في منتصفه.

## قواعد ثابتة — التزم بها حرفياً في هذه المرحلة

**التصميم (لا تحتاج فتح `DESIGN.md` — كل ما يلزمك هنا):**
- بطاقات: `rounded-2xl border border-border bg-card shadow-xs`
- مسافات: `p-3 gap-3` جوال · `md:p-6 md:gap-5` ديسكتوب
- عناوين `text-[14px] font-semibold` · نص مساند `text-[11.5px] text-muted-foreground`
- **لا نص أصغر من 11px · لا إيموجي · لا تمرير أفقي · كل زر `min-h-11`**
- تنسيقات منطقية فقط (`ms/me/ps/pe/start/end`) — ممنوع `left/right`
- **أرقام لاتينية دائماً** — لا `٠١٢٣` ولا تواريخ عربية-هندية
- أيقونة في بطاقة: حاوية 38–40px والرمز 19–20px · في زر أو رأس: 17–18px
  · شريط التنقّل 24px · زر «+» 58px · `stroke-width: 1.9`
- **إحساس تطبيق جوال**: كل مودال لوح سفلي، أزرار متجاورة لا مكدّسة،
  `active:scale-[.98]` عند الضغط، احترام `env(safe-area-inset-*)`
- كل المبالغ **بالهللات (integer)** — التحويل في `@/lib/format` فقط

**اقتصاد السياق:**
- لا تقرأ المشروع كاملاً. ابحث أولاً (grep) ثم افتح الملفات التي ظهرت فقط.
- لا تفتح ملفاً «لفهم المشروع أكثر». لا تقرأ `node_modules` · `vendor` · `dist` · lock files.
- لا تعِد قراءة ملف قرأته في هذه الجلسة إلا إن تغيّر.
- لا تشرح الكود لي ولا تعرض محتوى الملفات ولا diff كامل — نفّذ مباشرة.
- لا تعمل refactor خارج نطاق هذه المرحلة.
- استعمل المكوّنات الموجودة، ولا تعيد كتابة مكوّن المطلوب إعادة استخدامه فقط.
- ردودك أثناء التنفيذ مختصرة جداً.

**إن فشل نفس الإصلاح مرّتين:** توقّف، واذكر في ثلاثة أسطر: المشكلة · الملفات ·
ما جُرّب. لا تحاول ثالثة.

---

## ابدأ بالبحث عن (ثم افتح ما ظهر فيه منطق مالي فقط)

```
startOfMonth      whereMonth      format('Y-m')
BudgetGuard       DashboardController
```

`resources/js/lib/money.ts` **جاهز** — يحتوي منطق شهر الراتب في الواجهة.

---

## التنفيذ

### 1 · الخدمة أولاً

`app/Services/SalaryMonthService.php`:
`current()` → `{key, startsOn, endsOn, label, daysLeft, dayIndex}` ·
`periodFor($date)` · `previous()` · `incomeFor($key)` · `expensesFor($key)` ·
`close($key, $action)`

يوم البداية = `user->salary_day`. من راتبه يوم 1 لا يلاحظ أي فرق.
`label` = «راتب أغسطس» · `key` = `2026-08`.

اختبر الخدمة وحدها قبل أي استبدال.

### 2 · الاستبدال وحدة وحدة (لا دفعة واحدة)

بالترتيب، ومع تشغيل الاختبار الخاص بكل وحدة بعدها:
`BudgetGuard` → `Dashboard` → `Budgets` → `Commitments` → `Savings` → `Reports`

### 3 · إقفال الشهر

عند بدء راتب جديد ولم يُقفل السابق، يُفتح `SheetShell`:
- «فاض من راتب أغسطس **1,240 ر.س**» أو «تجاوزت بـ 320 ر.س»
- ثلاثة خيارات: **حوّلها لهدف ادخار** (المقترح) · **ضمّها لراتب سبتمبر** · **قسّمها**
- رابط «تقرير راتب أغسطس (PDF)» — يُفعَّل في المرحلة 7
- زر «ابدأ براتب سبتمبر»
- يُكتب الناتج في `salary_periods` مع `surplus_action`

الفائض **لا يُرحَّل صامتاً**: الترحيل الصامت يضخّم «المتبقي لك» فيتحوّل
الفائض إلى صرف بدل ادخار.

### 4 · المصطلح

«راتب أغسطس» في كل نص يراه المستخدم. **كلمة «دورة» ممنوعة تماماً.**

---

## التحقّق

```
php artisan test --compact
```

واختبار حاسم: بتاريخ 1 سبتمبر ويوم راتب 27 → اللوحة تعرض «راتب أغسطس»
و«المتبقي لك» يحسب من 27 أغسطس لا من 1 سبتمبر.

---

## عند الانتهاء

اعرض **فقط**:
1. ما نُفِّذ (نقاط قصيرة)
2. أسماء الملفات المتغيّرة
3. نتيجة التحقّق
4. أي شيء يمنع المرحلة التالية

ثم **توقّف. لا تبدأ المرحلة التالية.**

```
git add -A
git commit -m "feat(stage-4): salary month"
```
