# الدفعة 4أ — لوحة التحكم على الجوال

## المشكلة

`MoneyStoryCard` و `FinanceCalendar` مصمّمان للديسكتوب، فانحشرا على الجوال:

- «دخلك 990» يطفو بجانب الرقم البطل بلا سياق
- «متوسط صرفك اليومي 0» في صندوق كبير يأكل مساحة، وقيمته صفر أصلاً
- الأرقام الثلاثة متباعدة بمسافات ديسكتوب فتبدو مبعثرة
- مفتاح شريط التدفّق يعرض بنوداً قيمتها صفر
- التقويم بأربعة عشر يوماً يحتاج **تمرير أفقي** — وهذا ما نحاربه

---

## الحل

**`MoneyStoryCard` مُعاد بناؤها mobile-first:**

| على الجوال | على الديسكتوب |
|---|---|
| الرقم البطل 32px | 36px |
| الأرقام الثلاثة في **صف ثلاثي مضغوط** خلف فاصل | صف أفقي بمسافات واسعة |
| متوسط الصرف **سطر واحد** أسفل الشريط | بطاقة جانبية |
| المفتاح 10.5px بلفّ تلقائي | 12.5px |

**قواعد إخفاء الضجيج (في المقاسين):**
- قطع شريط التدفّق التي قيمتها صفر **لا تُرسم**
- مفتاح الرسم يعرض البنود **غير الصفرية فقط**
- «متوسط صرفك اليومي» يظهر **فقط بعد أول مصروف**
- لا التزامات ولا مصاريف → سطر واحد: «كل دخلك متاح لك»

**`UpcomingStrip` تحلّ محل `FinanceCalendar` في اللوحة:**

```
🟣  فاتورة الكهرباء     بعد يومين      450
🟠  قسط السيارة        بعد 5 أيام   1,000
🟢  الراتب             بعد 5 أيام   8,000
```

ثلاثة أسطر · صفر تمرير · نفس المعلومة. والتقويم الكامل ينتقل لصفحة
`/calendar` مستقلة يقدر يعدّل عليها ويشوفها بحجم أكبر.

---

## الملفات الجاهزة

```
resources/js/components/MoneyStoryCard.svelte  ← يستبدل الموجود
resources/js/components/UpcomingStrip.svelte   ← جديد
```

**لا تعيد كتابتهما.**

---

## الرسالة — الصقها في opencode

> **ابدأ التنفيذ الآن.** اجعل لوحة التحكم mobile-first.
>
> `MoneyStoryCard.svelte` (محدّث) و `UpcomingStrip.svelte` (جديد) موجودان
> وجاهزان — **لا تعيد كتابتهما ولا تعدّل تصميمهما**.
>
> **1 ·** في `resources/js/pages/Dashboard.svelte` استبدل `FinanceCalendar`
> بـ `UpcomingStrip` ومرّر لها نفس `calendarEvents`. احذف استيراد
> `FinanceCalendar` من الصفحة (أبقِ الملف نفسه — تستخدمه صفحة `/calendar`).
>
> **2 ·** رتّب لوحة التحكم للجوال بهذا الترتيب:
> 1. بطاقة «أكمل إعدادك» (إن وجدت)
> 2. `MoneyStoryCard`
> 3. `UpcomingStrip`
> 4. البطاقات المختصرة الأربع — `grid-cols-2` على الجوال و `lg:grid-cols-4`
> 5. «وين راحت مصاريفك؟» — على الجوال اعرض **القائمة فقط بلا الرسم الدائري**
>    (`hidden md:block` على الـ SVG). الرسم الدائري بقطر 132px يأكل نصف
>    الشاشة ويعطي معلومة أقل من القائمة.
> 6. «الدخل مقابل المصاريف» — `hidden md:block` على الجوال. الرسم بستة
>    أعمدة غير مقروء على 375px؛ رابط «شوف الاتجاه» يودّي `/reports`.
> 7. «ميزانيتك حسب الفئة» — `grid-cols-1` على الجوال
> 8. «آخر المعاملات» — بطاقات مكدّسة (النمط موجود أصلاً)
>
> **3 ·** المسافات: `gap-3` و `p-3` على الجوال، `md:gap-[18px]` و `md:p-6`
> على الديسكتوب. المسافات الحالية ديسكتوب في المقاسين.
>
> **4 ·** أنشئ صفحة `/calendar` جديدة:
> - `CalendarController` + `resources/js/pages/Calendar.svelte`
> - شهر كامل بشبكة 7 أعمدة، تنقّل بين الأشهر
> - النقر على يوم يفتح لوحاً سفلياً بأحداثه مع أزرار «تم الدفع» و«تعديل»
> - على الديسكتوب: الشبكة يمين، وتفاصيل اليوم المختار يسار
> - أعِد استخدام `FinanceCalendar.svelte` لعرض الأسبوعين داخل الصفحة
>
> **5 ·** شغّل `npm run build` وافحص على عرض **375px**: صفر تمرير أفقي في
> أي قسم من اللوحة.
>
> اقرأ `AGENTS.md` و `DESIGN.md` أولاً. لا تلمس أي ملف في
> `resources/js/components/` عدا ما ذُكر.
>
> نفّذ البنود الخمسة كاملة، وبعد الانتهاء اعرض ملخّص الملفات المتغيّرة.

---

## بعد التنفيذ

```
npm run build
composer run dev
```

افحص على **375px** و **440px** (آيفون 16 Pro Max):

| # | الاختبار | المتوقع |
|---|---|---|
| 1 | بطاقة «المتبقي لك» | رقم كبير + سطر يومي + شريط + صف ثلاثي مرتّب |
| 2 | حساب فاضي | لا يظهر «متوسط صرفك 0» ولا مفتاح فاضي |
| 3 | «القادم قريباً» | ثلاثة أسطر، بلا تمرير أفقي |
| 4 | تمرير الصفحة يميناً ويساراً | **لا يتحرك شيء** |
| 5 | «التقويم الكامل» | يفتح `/calendar` |

```
git add -A
git commit -m "feat: mobile-first dashboard + calendar page"
git push
```
