# الدفعة 4أ — لوحة التحكم + إصلاح التباين + تكبير الشريط السفلي

## ملاحظاتك المطبّقة

| الملاحظة | التنفيذ |
|---|---|
| «القادم قريباً» → **«التقويم المالي»** | تمّ |
| **احذف «وين راحت مصاريفك؟»** من اللوحة | تُنقل لصفحة التقارير |
| **احذف «ميزانيتك حسب الفئة»** من اللوحة | مكانها صفحة الميزانية |
| **آخر المعاملات** تظهر مباشرة | تمّ |
| **المربع العلوي كبير** | الرقم 40px ← **28px**، والحشوة 24px ← 14px |
| **أرقام باهتة غير واضحة** | إصلاح توكن اللون — انظر أدناه |
| **أيقونات الشريط السفلي صغيرة** | 21px ← **24px**، وزر [+] 52px ← **58px** |

---

## 🔴 إصلاح التباين — مقاس برمجياً

`--muted-foreground` الحالي `#8a8b90` يعطي تبايناً **3.09:1** على الخلفية.
الحد الأدنى المطلوب **4.5:1**. أي أن الأرقام الباهتة **مخالفة فعلياً**، لا
مسألة ذوق.

| اللون | على البطاقة | على الخلفية |
|---|---|---|
| `#8a8b90` الحالي | 3.40 ❌ | 3.09 ❌ |
| `#6b6c72` البديل | 5.23 ✅ | 4.75 ✅ |

**في `resources/css/app.css`:**

```css
:root {
    --muted-foreground: #6b6c72;   /* كان #8a8b90 */
}

.dark {
    --muted-foreground: #9a9ba3;   /* كان #83848a */
}
```

---

## 🔴 خلل الادخار

إضافة مبلغ لهدف ادخار **لا تنقص «المتبقي لك»** ولا تظهر في اللوحة.

**القرار المعتمد:** الإيداع **ينقص «المتبقي لك»** ويظهر كقطعة خضراء في شريط
التدفّق — و**لا يُسجَّل كمصروف** (فالفلوس ما راحت، انتقلت من جيب لجيب).

يُصلَح في الدفعة 4د مع باقي صفحة الادخار.

---

## الملفات الجاهزة

```
resources/js/components/MoneyStoryCard.svelte  ← نسخة مضغوطة
resources/js/components/UpcomingStrip.svelte   ← «التقويم المالي»
resources/js/components/AppNav.svelte          ← أيقونات أكبر
```

**لا تعيد كتابتها.**

---

## الرسالة — الصقها في opencode

> **ابدأ التنفيذ الآن.**
>
> `MoneyStoryCard.svelte` و `UpcomingStrip.svelte` و `AppNav.svelte`
> محدّثة وجاهزة — **لا تعيد كتابتها ولا تعدّل تصميمها**.
>
> **1 · إصلاح التباين (مهم)**
> في `resources/css/app.css`:
> - في `:root` غيّر `--muted-foreground` إلى `#6b6c72`
> - في `.dark` غيّر `--muted-foreground` إلى `#9a9ba3`
>
> القيمة القديمة `#8a8b90` تعطي تبايناً 3.09:1 والحد الأدنى 4.5:1.
>
> ثم ابحث في كل ملفات `.svelte` عن أي نص بحجم **أصغر من 11px** وارفعه إلى
> `11px` كحد أدنى. واستبدل `text-foreground/70` و `text-foreground/75` بـ
> `text-foreground/80`.
>
> **2 · لوحة التحكم — احذف قسمين**
> في `resources/js/pages/Dashboard.svelte`:
> - **احذف** قسم «وين راحت مصاريفك؟» بالكامل (بما فيه `CategoryDonut`)
> - **احذف** قسم «ميزانيتك حسب الفئة» بالكامل (بما فيه `BudgetRow`)
> - **احذف** قسم «الدخل مقابل المصاريف» (`MonthlyBars`)
>
> **لا تحذف ملفات المكوّنات نفسها** — `CategoryDonut` و `MonthlyBars`
> ستُستخدمان في صفحة التقارير، و`BudgetRow` في صفحة الميزانية.
>
> **3 · لوحة التحكم — الترتيب النهائي**
> 1. بطاقة «أكمل إعدادك» (إن وجدت)
> 2. `MoneyStoryCard`
> 3. `UpcomingStrip` (استبدل بها `FinanceCalendar`، ومرّر نفس `calendarEvents`)
> 4. البطاقات المختصرة الأربع — `grid-cols-2` جوال و `lg:grid-cols-4` ديسكتوب
> 5. «آخر المعاملات»
>
> المسافات: `gap-3 p-3` على الجوال، `md:gap-5 md:p-6` على الديسكتوب.
>
> **4 · صفحة التقارير**
> أضف إلى `resources/js/pages/Reports.svelte` قسم **«وين راحت مصاريفك؟»**
> باستخدام `CategoryDonut`، وقسم **«الدخل مقابل المصاريف»** باستخدام
> `MonthlyBars`. مرّر لهما البيانات من `ReportsController`.
>
> **5 · صفحة التقويم**
> أنشئ `/calendar`: `CalendarController` + `resources/js/pages/Calendar.svelte`.
> شهر كامل بشبكة 7 أعمدة، تنقّل بين الأشهر، والنقر على يوم يفتح لوحاً
> سفلياً بأحداثه مع أزرار «تم الدفع» و«تعديل». أعِد استخدام
> `FinanceCalendar.svelte` لعرض الأسبوعين داخل الصفحة.
>
> **6 · تحقّق**
> `npm run build` ثم افحص على عرض **375px** و **440px**: صفر تمرير أفقي،
> وكل نص مقروء.
>
> اقرأ `AGENTS.md` و `DESIGN.md` أولاً. لا تلمس أي ملف في
> `resources/js/components/` عدا ما ذُكر.
>
> نفّذ البنود الستة كاملة، وبعد الانتهاء اعرض ملخّص الملفات المتغيّرة.

---

## اللوحة بعد التنفيذ

```
┌──────────────────────────────┐
│ لوحة التحكم        [مساعد]   │
├──────────────────────────────┤
│  المتبقي لك للصرف            │
│  3,480 ر.س                    │
│  تقدر تصرف 268 يومياً         │
│  ────────────────────────     │
│  دخلك   محجوز   صرفت         │
│  ▓▓▓▓░░░░░░ شريط التدفّق      │
├──────────────────────────────┤
│  التقويم المالي   [الكامل ←]  │
│  🟣 كهرباء    بعد يومين  450  │
│  🟠 قسط سيارة  بعد 5 أيام 1,000│
├──────────────────────────────┤
│  [الدخل] [المصاريف]           │
│  [المحجوز] [الادخار]          │
├──────────────────────────────┤
│  آخر المعاملات                │
└──────────────────────────────┘
```

أربعة أقسام بدل سبعة.

```
git add -A
git commit -m "feat: lean mobile dashboard + contrast fix"
git push
```

---

## الدفعات القادمة

| الدفعة | المحتوى |
|---|---|
| **4ب** | صفحة الميزانية — حذف شريط الترتيب · توضيح أرقام الفئات · «غير مخصّص» بارز · زر تعديل واضح |
| **4ج** | صفحة الالتزامات — إعادة تصميم · إضافة فاتورة واضحة · «كم استهلك» |
| **4د** | صفحة الادخار — حذف التكرار · إجمالي مضغوط · إيداع سريع · **إصلاح الخصم من المتبقي** |
