# صفحة الالتزامات — الدفعة الكاملة

> فواتير · إيجارات · أقساط · اشتراكات في صفحة واحدة، مع منطق «محجوز/مدفوع»،
> والخصم التلقائي مقابل اليدوي، والتحقّقات الحقيقية.

---

## القاعدة الحاكمة

| النوع | طبيعته | القرار |
|---|---|---|
| فاتورة · إيجار · اشتراك | **واقعة** تصلك سواء رضيت أو لا | **لا تُمنع أبداً** — تُحذَّر فقط |
| قسط | **قرار** تأخذه بإرادتك | **يُمنع** إذا فاق الدخل، مع اقتراح بديل |

منع تسجيل فاتورة وصلتك فعلاً = المستخدم لن يسجّلها، فتصير أرقام التطبيق خيالاً.

---

## محجوز مقابل مدفوع

- **محجوز**: مبلغ محدَّد لم يُدفع بعد → **مطروح من «المتبقي لك»**، لكن الفلوس
  لا تزال في الحساب.
- **مدفوع**: خرج فعلاً → ينتقل للجزء الأخضر في شريط الصحة.

الفاتورة المتغيّرة بلا مبلغ مسجَّل تُحجَز بـ **متوسّط آخر 3 أشهر** لا بصفر،
وإلا انتفخ «المتبقي لك» بمبلغ سيخرج حتماً.

---

## الملفات الجاهزة (لا تُعِد كتابتها)

```
resources/js/lib/commitments.ts                       ← كل المنطق المشترك
resources/js/components/CommitmentCard.svelte         ← بطاقة التزام موحّدة
resources/js/components/CommitmentHealth.svelte       ← شريط الصحة
resources/js/components/CommitmentTypeCard.svelte     ← بطاقة نوع = فلتر
resources/js/components/AddCommitmentSheet.svelte     ← لوح الإضافة
resources/js/components/FreedomCard.svelte            ← «يوم تحرّرك»
resources/js/pages/Commitments.svelte                 ← الصفحة
```

---

## الرسالة — الصقها في opencode

> **ابدأ التنفيذ الآن.** بناء صفحة الالتزامات كاملة. ملفات الواجهة في
> `resources/js/` **جاهزة وموجودة — لا تعيد كتابتها ولا تعدّل تصميمها**.
> مهمّتك: الباك-إند والربط والتحقّقات.
>
> ═══════════════════════════════════════════════════
>
> ### 1 · قاعدة البيانات
>
> **`commitments`**
>
> | العمود | النوع | ملاحظات |
> |---|---|---|
> | `user_id` | foreignId | |
> | `kind` | enum | `bill,rent,installment,subscription` |
> | `name` | string | |
> | `icon` · `color` | string | افتراضياً حسب النوع |
> | `amount` | bigInteger **nullable** | هللات · المبلغ الشهري · null للمتغيّرة |
> | `is_variable` | boolean | للفواتير فقط |
> | `total_amount` | bigInteger default 0 | للأقساط |
> | `months_count` · `months_paid` | integer default 0 | |
> | `payment_method` | enum | `auto,manual` |
> | `due_type` | enum | `salary_day,month_day,fixed_date` |
> | `due_day` | tinyInteger nullable | 1..31 |
> | `due_date` | date nullable | لـ `fixed_date` |
> | `notify_before` · `notify_on_due` · `notify_late` | boolean | |
> | `reserve_in_budget` | boolean default true | |
> | `is_active` | boolean default true | الأرشفة بدل الحذف |
>
> **`commitment_payments`**: `commitment_id` · `amount` (هللات) ·
> `paid_at` · `period_key` (مفتاح شهر الراتب مثل `2026-08`) ·
> `source` enum(`auto`,`manual`) · فهرس فريد `(commitment_id, period_key)`.
>
> **رحّل** جداول `bills` و `installments` القديمة إلى `commitments`
> (`kind='bill'` و `kind='installment'`) داخل migration، ثم أبقِ المسارات
> القديمة كإعادة توجيه إلى `/commitments`.
>
> ### 2 · التحقّقات — نفّذها كما هي بالضبط
>
> استخدم `ValidationException::withMessages()` دائماً. **ممنوع** `abort(422)`.
>
> **عند الإضافة/التعديل:**
>
> 1. `name` مطلوب.
> 2. `amount` مطلوب **إلا** إذا `is_variable = true` (والمتغيّر مسموح
>    للفواتير فقط — الإيجار والاشتراك والقسط مبالغها معروفة).
> 3. قسط: `months_count` بين **2 و 480**، و`total_amount > 0`.
>    **القسط الشهري يُحسب في الخادم** `ceil(total_amount / months_count)`
>    ولا يُقبل من العميل إطلاقاً.
> 4. `due_type=month_day` → `due_day` بين 1 و 31.
>    `due_type=fixed_date` → `due_date` مطلوب ومستقبلي.
>    `due_type=salary_day` → يتطلّب أن يكون `salary_day` محدّداً للمستخدم،
>    وإلا رسالة: «حدّد يوم نزول راتبك أولاً من الإعدادات».
> 5. **المنع الوحيد** — قسط جديد يجعل مجموع الالتزامات الشهرية يفوق الدخل:
>    ارفض برسالة: «هذا القسط يرفع التزاماتك إلى X وهي أكبر من دخلك Y —
>    مدّه على أشهر أكثر أو أنهِ التزاماً قائماً أولاً».
>    استخدم `BudgetGuard::assertCommitmentFits` **لـ `kind=installment` فقط**.
> 6. فاتورة/إيجار/اشتراك تتجاوز الدخل: **تُحفظ**، ويُمرَّر تحذير في
>    `flash('warnings')` تعرضه الواجهة.
>
> **عند الدفع (`POST /commitments/{c}/pay`):**
>
> 7. الفاتورة المتغيّرة: `amount` **مطلوب** عند الدفع.
> 8. لا يُقبل دفع مرّتين في نفس `period_key` (الفهرس الفريد + رسالة واضحة).
> 9. قسط: `months_paid` لا يتجاوز `months_count` أبداً؛ عند التساوي
>    اجعل `is_active=false` وأظهر «خلص».
> 10. الدفع يُنشئ صفّاً في `commitment_payments` **ولا يُنشئ `expense`** —
>     الالتزامات فئة مستقلّة في التقارير، وتسجيلها كمصروف يكرّر الرقم مرّتين.
> 11. إذا تجاوز الدفع «المتبقي لك» → **لا تمنع**، مرّر المستخدم على نفس مسار
>     `ExpenseFundingService` («من وين جاء المبلغ؟»).
>
> **عند التعديل:**
>
> 12. تعديل `months_count` أو `total_amount` لقسط بدأت أقساطه: يُسمح
>     بتعديل **المتبقي فقط**، مع رسالة توضّح أن الأشهر المدفوعة لا تتغيّر.
> 13. **الحذف = أرشفة** (`is_active=false`) إذا له مدفوعات. تأكيد الحذف
>     يعرض اسم الالتزام.
>
> ### 3 · الخصم التلقائي مقابل اليدوي
>
> أمر مجدول يومي `commitments:post-due`:
>
> - `payment_method=auto` واستحقّ اليوم → أنشئ دفعة `source=auto` وأشعر:
>   «انخصم قسط السيارة — 1,000 ر.س» مع زر **«ما انخصم»** يحذف الدفعة.
> - **شرط حاسم:** الالتزام المربوط بـ `salary_day` **لا يُسجَّل تلقائياً إلا
>   بعد تسجيل دخل شهر الراتب فعلاً**. لو تأخّر الراتب، لا يفترض التطبيق
>   خصماً لم يحدث.
> - `payment_method=manual` → يبقى **محجوزاً** ويظهر سؤال «هل سددت X؟»
>   بزرّين: «نعم، دفعت» · «لسّه».
> - التنبيهات حسب مفاتيح `notify_*`: قبل 3 أيام · يوم الاستحقاق · عند التأخّر.
>
> ### 4 · الربط بالميزانية
>
> - `BudgetGuard::obligations()` = مدفوعات هذا الشهر + **المحجوز** (الالتزامات
>   غير المدفوعة التي `reserve_in_budget=true`، وبمتوسّطها للمتغيّرة).
> - لوحة التحكم: قطعة في شريط التدفّق باسم «التزامات» بلون `--chart-7`،
>   وتُطرح من «المتبقي لك».
> - `Commitments.svelte` يستقبل: `commitments` · `income` · `salaryDay` ·
>   `periodLabel` (مثل «راتب أغسطس»). كل المبالغ **بالهللات**.
>
> ### 5 · الإشعارات (sonner · `position="top-center"`)
>
> | الحدث | النص |
> |---|---|
> | إضافة | «تمت إضافة فاتورة «كهرباء»» |
> | دفع | «تم دفع كهرباء — 310 ر.س» + زر تراجع 5 ثوانٍ |
> | تراجع | «رجّعنا كهرباء إلى محجوز» |
> | خصم تلقائي | «انخصم قسط السيارة — 1,000 ر.س» + «ما انخصم» |
> | آخر قسط | «خلص قسط اللابتوب — يرجع لك 400 ر.س شهرياً» |
>
> ### 6 · التصميم — التزام حرفي
>
> - المسافات `p-3 gap-3` جوال · `md:p-6` ديسكتوب · البطاقات `rounded-2xl border border-border bg-card shadow-xs`
> - **لا نص أصغر من 11px · صفر إيموجي · صفر تمرير أفقي · كل زر `min-h-11`**
> - **لا زر عائم** — زر «إضافة» في رأس الصفحة فقط (الزر العائم كان يغطّي المحتوى)
> - الأسماء والأسطر الثانوية **تُقصّ ولا تلتفّ** (`truncate`) حتى تتساوى
>   ارتفاعات البطاقات
> - لوح الإضافة: سفلي على الجوال (`items-end` + `rounded-t-3xl`)، مركزي على
>   الديسكتوب (`md:items-center`)، يُغلق بـ `Esc`
>
> ### 7 · تحقّق
>
> `php artisan migrate` ← `npm run build` ← `php artisan test --compact`،
> ثم افحص على **375px** و **440px**.
>
> ═══════════════════════════════════════════════════
>
> اقرأ `AGENTS.md` و `DESIGN.md` أولاً. المبالغ بالهللات.
> بعد الانتهاء اعرض ملخّص الملفات المتغيّرة.

---

## اختبر بعدها

| # | الاختبار | المتوقع |
|---|---|---|
| 1 | أضف فاتورة كهرباء متغيّرة | تُحفظ · تُحجز بالمتوسّط · إشعار |
| 2 | أضف قسط 36,000 على 36 شهر | القسط الشهري يظهر **1,000 محسوباً** لا مكتوباً |
| 3 | أضف قسطاً أكبر من دخلك | **يُرفض** برسالة تقترح مدّ الأشهر |
| 4 | أضف إيجاراً أكبر من دخلك | **يُحفظ** مع تحذير برتقالي |
| 5 | اضغط «تم الدفع» | ينتقل من محجوز لمدفوع · «المتبقي لك» لا يتغيّر (كان محجوزاً) |
| 6 | اضغط «تراجع» | يرجع محجوزاً |
| 7 | افتح الصفحة على 375px | صفر تمرير أفقي · كل البطاقات بنفس الارتفاع |

```
git add -A
git commit -m "feat(commitments): unified commitments page with reserved/paid logic"
git push
```
